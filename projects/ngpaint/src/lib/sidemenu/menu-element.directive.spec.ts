import { MenuElementDirective } from './menu-element.directive';

describe('MenuElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuElementDirective();
    expect(directive).toBeTruthy();
  });
});
