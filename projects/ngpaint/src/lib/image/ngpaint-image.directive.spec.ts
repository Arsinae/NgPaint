import { NgpaintImageDirective } from './ngpaint-image.directive';

describe('NgpaintImageDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpaintImageDirective();
    expect(directive).toBeTruthy();
  });
});
